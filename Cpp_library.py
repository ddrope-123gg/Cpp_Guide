cpp_library = {'C++ variables': {'type': 'C++ variables & data types',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             'int main() {\n'
                             '    int stockQuantity = 42;             // Integer (whole number '
                             'count)\n'
                             '    float unitPrice = 12.99f;           // Floating point number '
                             "(decimal price, 'f' suffix marks it as float)\n"
                             '    double preciseWeight = 3.14159265;  // Double (higher precision '
                             'decimal than float)\n'
                             "    char warehouseZone = 'B';           // Character (single quote, "
                             'one letter/symbol)\n'
                             '    bool isRestockNeeded = true;        // Boolean (true or false '
                             'toggle)\n'
                             '    string productName = "Steel Bolts"; // String (double quotes, '
                             'sequence of characters, needs #include <string>)\n'
                             '\n'
                             '    cout << "Product: " << productName << " | Qty: " << '
                             'stockQuantity << " | Zone: " << warehouseZone << endl;\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Variables are named storage locations in memory that hold data for a '
                           'program to use. C++ is a statically-typed language, which means every '
                           'variable must be declared with an explicit data type before it can be '
                           'used, and that type cannot change afterward. When you declare a '
                           'variable you are telling the compiler exactly how much memory to '
                           'reserve and how to interpret the bits stored there. The general syntax '
                           "follows the pattern 'type variableName = value;'. Choosing the right "
                           'type matters for both correctness (a char cannot hold a whole word) '
                           'and efficiency (an int uses less memory than a double for a simple '
                           'counter).'},
               'C++ data types': {'type': 'C++ variables & data types',

                    'syntax': '#include <iostream>\n'
                              'using namespace std;\n'
                              '\n'
                              'int main() {\n'
                              '    int characterLevel = 12;              // int: whole numbers, '
                              'typically 4 bytes\n'
                              '    long long totalExperiencePoints = 9876543210LL; // long long: '
                              "for very large whole numbers beyond int's range\n"
                              '    float criticalHitChance = 27.5f;      // float: single '
                              'precision decimal, ~6-7 digits accuracy\n'
                              '    double damageMultiplier = 1.755123456; // double: double '
                              'precision decimal, ~15 digits accuracy\n'
                              "    char playerRank = 'S';                // char: a single "
                              'character, stored as 1 byte\n'
                              '    bool isAlive = true;                  // bool: only true or '
                              'false, stored as 1 byte\n'
                              '    string guildName = "Iron Wolves";     // string: text of any '
                              'length (from the <string> library)\n'
                              '\n'
                              '    cout << "Level " << characterLevel << " | XP: " << '
                              'totalExperiencePoints << " | Rank: " << playerRank << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': 'C++ provides several built-in (primitive) data types, each designed '
                            "for a specific kind of value and memory footprint. 'int' stores whole "
                            "numbers, 'float' and 'double' store decimal numbers with double "
                            "offering roughly twice the precision of float, 'char' stores a single "
                            "character in one byte, and 'bool' stores only true or false. The "
                            "'string' type is not a primitive but comes from the Standard "
                            "Library's <string> header and is used for storing sequences of "
                            'characters. Picking the smallest type that safely fits your expected '
                            'range of values is a common practice for memory-conscious C++ '
                            'programs, especially in performance-critical or embedded contexts.'},
 'C++ type conversion': {'type': 'C++ variables & data types',
                         'syntax': '#include <iostream>\n'
                                   'using namespace std;\n'
                                   '\n'
                                   'int main() {\n'
                                   '    // 1. Implicit conversion (automatic, done by the '
                                   'compiler)\n'
                                   '    int wholeDegrees = 24;\n'
                                   '    double autoConverted = wholeDegrees;   // int '
                                   'automatically becomes double: 24.0, no data is lost\n'
                                   '\n'
                                   '    // 2. Explicit conversion / casting (manual, done by the '
                                   'programmer)\n'
                                   '    double preciseReading = 24.98;\n'
                                   '    int truncatedReading = (int) preciseReading;   // C-style '
                                   'cast: chops off the decimal part, becomes 24\n'
                                   '    int modernCastReading = static_cast<int>(preciseReading); '
                                   '// Modern C++ recommended cast, same result\n'
                                   '\n'
                                   '    cout << "Auto: " << autoConverted << " | Truncated: " << '
                                   'truncatedReading << " | Modern cast: " << modernCastReading << '
                                   'endl;\n'
                                   '    return 0;\n'
                                   '}',
                         'desc': 'Type conversion is the act of changing a value from one data '
                                 'type to another. Implicit conversion happens automatically '
                                 'whenever the compiler can safely widen a smaller type into a '
                                 'larger one, such as turning an int into a double, because no '
                                 'information is lost in the process. Explicit conversion, '
                                 'commonly called casting, must be written directly by the '
                                 "programmer using either the older C-style syntax '(type)value' "
                                 'or the safer, more descriptive modern syntax '
                                 "'static_cast<type>(value)'. Casting is typically used to narrow "
                                 'a larger type into a smaller one, such as squeezing a double '
                                 'into an int, which truncates (chops off) any decimal portion and '
                                 'can lead to data loss if not handled carefully.'},
 'C++ constants': {'type': 'C++ variables & data types',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             'int main() {\n'
                             "    const double GRAVITY_ACCELERATION = 9.81;  // 'const' locks the "
                             'value permanently after this line\n'
                             '    const int MAX_PLAYERS_ALLOWED = 4;\n'
                             '\n'
                             '    // GRAVITY_ACCELERATION = 10.0;   // This line would cause a '
                             'compile error if uncommented\n'
                             '\n'
                             '    double objectWeight = 70.0 * GRAVITY_ACCELERATION;\n'
                             '    cout << "Simulated weight force: " << objectWeight << " Newtons '
                             'for up to " << MAX_PLAYERS_ALLOWED << " players." << endl;\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Constants are variables whose value is fixed at the moment of '
                           'declaration and can never be changed afterward during program '
                           "execution. In C++ you create one by placing the 'const' keyword before "
                           'the data type. Attempting to reassign a const variable anywhere later '
                           'in the code will cause the compiler to reject the program with an '
                           'error, which makes constants extremely useful for values that must '
                           'never accidentally change, such as mathematical constants, '
                           'configuration limits, or fixed conversion rates. Using constants also '
                           "documents intent clearly for anyone reading the code, signaling 'this "
                           "value is meant to stay the same.'"},
 'C++ operators': {'type': 'C++ operators & math',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             'int main() {\n'
                             '    int itemCount = 5;\n'
                             '    double pricePerItem = 8.50;\n'
                             '\n'
                             '    // Arithmetic operators\n'
                             '    double subtotal = itemCount * pricePerItem;   // Multiplication '
                             '(*)\n'
                             '    int leftoverPoints = 47 % 10;                  // Modulo (%): '
                             'remainder after division, gets remaining loyalty points (7)\n'
                             '\n'
                             '    // Comparison operators return a bool\n'
                             '    bool qualifiesForDiscount = subtotal >= 40.0;  // Greater than '
                             'or equal to (>=)\n'
                             '\n'
                             '    // Compound assignment operators (shorthand updates)\n'
                             '    double finalTotal = subtotal;\n'
                             '    if (qualifiesForDiscount) {\n'
                             '        finalTotal -= 5.0;   // Shorthand for finalTotal = '
                             'finalTotal - 5.0\n'
                             '    }\n'
                             '\n'
                             '    cout << "Subtotal: " << subtotal << " | Discount applied: " << '
                             'qualifiesForDiscount << " | Final: " << finalTotal << endl;\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Operators are symbols that instruct the compiler to perform specific '
                           'operations on variables and values. Arithmetic operators (+, -, *, /, '
                           '%) perform mathematical calculations, where the modulo operator (%) '
                           'returns only the remainder left over after a division. Comparison '
                           'operators (==, !=, >, <, >=, <=) evaluate two values and always '
                           'produce a bool result of true or false. Compound assignment operators '
                           '(+=, -=, *=, /=) combine a math operation and an assignment into a '
                           "single shorthand step, so writing 'x += 5' is functionally identical "
                           "to writing 'x = x + 5', just more concise."},
 'C++ math': {'type': 'C++ operators & math',
              'syntax': '#include <iostream>\n'
                        '#include <cmath>   // Required for math functions\n'
                        'using namespace std;\n'
                        '\n'
                        'int main() {\n'
                        '    double launchVelocity = -45.7;\n'
                        '    double impactSurfaceHeight = 64.0;\n'
                        '\n'
                        '    double absoluteSpeed = abs(launchVelocity);        // abs(): strips '
                        'the negative sign, returns 45.7\n'
                        '    double landingDistance = sqrt(impactSurfaceHeight); // sqrt(): '
                        'calculates the square root, returns 8.0\n'
                        '    double poweredForce = pow(3.0, 4.0);                // pow(base, '
                        'exponent): raises 3 to the power of 4, returns 81.0\n'
                        '    double roundedUp = ceil(4.05);                       // ceil(): '
                        'rounds up to the nearest whole number, returns 5.0\n'
                        '    double roundedDown = floor(4.95);                     // floor(): '
                        'rounds down to the nearest whole number, returns 4.0\n'
                        '\n'
                        '    cout << "Speed: " << absoluteSpeed << " | Distance: " << '
                        'landingDistance << " | Force: " << poweredForce << endl;\n'
                        '    return 0;\n'
                        '}',
              'desc': 'The <cmath> header is part of the C++ Standard Library and provides a '
                      'collection of ready-made functions for performing common mathematical '
                      'operations that go beyond the basic arithmetic operators. Functions like '
                      'sqrt() compute square roots, pow() raises a number to a given exponent, '
                      'abs() returns the absolute (non-negative) value of a number, and '
                      'ceil()/floor() round decimal numbers up or down to the nearest whole number '
                      'respectively. Because these are free functions rather than methods tied to '
                      'an object, you call them directly by name after including the <cmath> '
                      'header at the top of your file.'},
 'C++ strings': {'type': 'C++ strings',
                 'syntax': '#include <iostream>\n'
                           '#include <string>\n'
                           'using namespace std;\n'
                           '\n'
                           'int main() {\n'
                           '    string rawInput = "  John_Doe123  ";\n'
                           '\n'
                           '    int len = rawInput.length();                 // Length of the '
                           'string, including spaces\n'
                           '    string upper = rawInput;\n'
                           '    for (char &c : upper) c = toupper(c);          // Manual uppercase '
                           'conversion (C++ has no built-in .toUpperCase())\n'
                           '\n'
                           '    size_t underscorePos = rawInput.find("_");     // find(): returns '
                           'the index of the first match, or string::npos if not found\n'
                           '    string firstName = rawInput.substr(2, 4);       // '
                           'substr(startIndex, length): extracts a slice of the string\n'
                           '    string combined = rawInput + " - verified";      // \'+\' '
                           'concatenates (joins) two strings together\n'
                           '    bool hasDigits = rawInput.find_first_of("0123456789") != '
                           'string::npos; // Checks if any digit exists\n'
                           '\n'
                           '    cout << "Length: " << len << " | Underscore at: " << underscorePos '
                           '<< " | Combined: " << combined << endl;\n'
                           '    return 0;\n'
                           '}',
                 'desc': "The C++ 'string' class, provided by the <string> header, represents a "
                         'sequence of characters and offers a rich set of member functions for '
                         'working with text. The length() function reports how many characters a '
                         'string holds, find() searches for a substring and returns its starting '
                         'index (or the special constant string::npos if nothing is found), '
                         'substr() extracts a portion of a string given a starting position and '
                         "length, and the '+' operator concatenates strings together. Unlike some "
                         'other languages, C++ strings do not have a built-in method to change '
                         'case directly, so tasks like uppercasing typically loop through each '
                         'character and transform it individually using functions like toupper() '
                         'from the <cctype> header.'},
 'C++ booleans': {'type': 'C++ control flow',
                  'syntax': '#include <iostream>\n'
                            'using namespace std;\n'
                            '\n'
                            'int main() {\n'
                            '    int employeeClearanceLevel = 3;\n'
                            '    int requiredClearanceLevel = 4;\n'
                            '\n'
                            '    bool hasSufficientClearance = employeeClearanceLevel >= '
                            'requiredClearanceLevel; // Comparison produces a bool (false)\n'
                            '    bool isBadgeScanned = true;\n'
                            '\n'
                            '    // Combined logical check using logical AND (&&)\n'
                            '    bool doorUnlocks = hasSufficientClearance && isBadgeScanned; // '
                            'True ONLY if BOTH conditions are true\n'
                            '\n'
                            '    cout << "Door unlocks: " << doorUnlocks << endl; // Prints as 1 '
                            '(true) or 0 (false) by default\n'
                            '    return 0;\n'
                            '}',
                  'desc': 'A bool is a data type that can hold exactly one of two possible values: '
                          'true or false. Booleans are the foundation of decision-making logic in '
                          'C++ and are most commonly produced automatically as the result of '
                          'comparison operators such as ==, !=, >, or <. Multiple boolean '
                          'conditions can be combined using logical operators: && (AND) requires '
                          'every condition to be true, || (OR) requires at least one condition to '
                          'be true, and ! (NOT) flips a value from true to false or vice versa. By '
                          'default, C++ prints true as 1 and false as 0 unless you explicitly '
                          "configure the output stream with 'boolalpha' to display the words "
                          'instead.'},
 'C++ if...else': {'type': 'C++ control flow',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             'int main() {\n'
                             '    int soilMoisturePercent = 22;\n'
                             '\n'
                             '    if (soilMoisturePercent < 30) {\n'
                             '        cout << "Action: Soil too dry! Activating sprinkler system." '
                             '<< endl;\n'
                             '    } else if (soilMoisturePercent > 80) {\n'
                             '        cout << "Action: Soil oversaturated! Pausing irrigation '
                             'cycle." << endl;\n'
                             '    } else {\n'
                             '        cout << "Status: Moisture levels optimal. Standing by." << '
                             'endl;\n'
                             '    }\n'
                             '\n'
                             '    // Ternary operator shorthand (condition ? value_if_true : '
                             'value_if_false)\n'
                             '    string alertLevel = (soilMoisturePercent < 10) ? "CRITICAL" : '
                             '"NORMAL";\n'
                             '    cout << "Alert level: " << alertLevel << endl;\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Conditional statements let a program choose which block of code to '
                           'execute based on whether a condition evaluates to true or false. The '
                           "'if' block runs only when its condition is true; an optional 'else if' "
                           'lets you test additional conditions in sequence if the first one '
                           "fails; and an optional final 'else' catches every remaining case where "
                           'none of the previous conditions matched. Only one branch of the entire '
                           'if/else-if/else chain will ever execute per pass. For simple '
                           "two-outcome decisions, the ternary operator ' ? : ' offers a compact "
                           'single-line alternative to a full if-else block, evaluating the '
                           'condition once and returning one of two values.'},
 'C++ switch': {'type': 'C++ control flow',
                'syntax': '#include <iostream>\n'
                          'using namespace std;\n'
                          '\n'
                          'int main() {\n'
                          '    int floorButtonPressed = 2;\n'
                          '\n'
                          '    switch (floorButtonPressed) {\n'
                          '        case 1:\n'
                          '            cout << "Elevator Control: Doors opening at Ground Floor." '
                          '<< endl;\n'
                          '            break;  // break exits the switch, stopping it from falling '
                          'into the next case\n'
                          '        case 2:\n'
                          '            cout << "Elevator Control: Ascending to Floor 2 - Offices." '
                          '<< endl;\n'
                          '            break;\n'
                          '        case 3:\n'
                          '            cout << "Elevator Control: Ascending to Floor 3 - Rooftop '
                          'Garden." << endl;\n'
                          '            break;\n'
                          '        default:  // Runs automatically if no case matches '
                          'floorButtonPressed\n'
                          '            cout << "Elevator Control: Invalid floor request. Returning '
                          'to lobby." << endl;\n'
                          '    }\n'
                          '    return 0;\n'
                          '}',
                'desc': 'A switch statement evaluates a single expression once and then jumps '
                        "directly to the matching 'case' label, offering a cleaner alternative to "
                        'writing a long chain of if-else-if statements when comparing one variable '
                        "against many possible fixed values. Each case must end with a 'break' "
                        "statement, or execution will 'fall through' and continue running the code "
                        'in the next case block regardless of whether it matches, which is a '
                        "common source of bugs for beginners. The optional 'default' case acts as "
                        'a catch-all that runs automatically whenever none of the listed case '
                        'values match the switch expression.'},
 'C++ while loop': {'type': 'C++ loops',
                    'syntax': '#include <iostream>\n'
                              'using namespace std;\n'
                              '\n'
                              'int main() {\n'
                              '    int batteryChargePercent = 0;\n'
                              '\n'
                              '    // Condition is checked BEFORE every single pass through the '
                              'loop\n'
                              '    while (batteryChargePercent < 100) {\n'
                              '        batteryChargePercent += 20; // Must update the counter, or '
                              'the loop runs forever\n'
                              '        cout << "Charging... " << batteryChargePercent << "% '
                              'complete." << endl;\n'
                              '    }\n'
                              '\n'
                              '    cout << "Battery fully charged. Safe to unplug." << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': 'A while loop repeatedly executes a block of code for as long as its '
                            'condition continues to evaluate to true, checking that condition '
                            'before every single iteration. This makes while loops ideal for '
                            'situations where you do not know in advance exactly how many times '
                            'the loop needs to repeat, only the condition under which it should '
                            'stop. Because the condition is checked first, it is possible for a '
                            'while loop to run zero times if the condition starts out false. It is '
                            'essential to modify some variable inside the loop body that affects '
                            'the condition, otherwise the loop will never end, resulting in what '
                            'is called an infinite loop.'},
 'C++ for loop': {'type': 'C++ loops',
                  'syntax': '#include <iostream>\n'
                            '#include <string>\n'
                            'using namespace std;\n'
                            '\n'
                            'int main() {\n'
                            '    // 1. Standard for loop: initialization; condition; increment '
                            'step\n'
                            '    for (int rowIndex = 0; rowIndex < 5; rowIndex++) {\n'
                            '        cout << "Auditing data row: " << rowIndex << endl;\n'
                            '    }\n'
                            '\n'
                            '    // 2. Range-based for loop (C++11+): ideal for stepping through '
                            'every item in a collection\n'
                            '    string serverNames[3] = {"Auth-01", "Cache-02", "Backup-03"};\n'
                            '    for (string name : serverNames) {\n'
                            '        cout << "Pinging server: " << name << endl;\n'
                            '    }\n'
                            '    return 0;\n'
                            '}',
                  'desc': 'A standard for loop is used when the exact number of repetitions is '
                          'known ahead of time. It packages three parts into one line separated by '
                          'semicolons: an initialization statement that runs once at the start, a '
                          'condition checked before every pass, and an increment (or decrement) '
                          'statement that runs at the end of every pass. A range-based for loop, '
                          'introduced in C++11, is a simplified variant designed specifically for '
                          'cycling through every element of an array or container without manually '
                          'tracking an index, making the code shorter and less error-prone when '
                          "the goal is simply 'do this for every item.'"},
 'C++ break': {'type': 'C++ loops',
               'syntax': '#include <iostream>\n'
                         'using namespace std;\n'
                         '\n'
                         'int main() {\n'
                         '    int targetTrackingId = 7734;\n'
                         '    int shipmentIds[5] = {5521, 3390, 7734, 8802, 1145};\n'
                         '\n'
                         '    for (int i = 0; i < 5; i++) {\n'
                         '        cout << "Scanning shipment slot: " << i << endl;\n'
                         '        if (shipmentIds[i] == targetTrackingId) {\n'
                         '            cout << "Match found at index " << i << "! Halting scan '
                         'immediately." << endl;\n'
                         '            break; // Immediately exits the loop entirely, skipping any '
                         'remaining iterations\n'
                         '        }\n'
                         '    }\n'
                         '    cout << "Scan process finished." << endl;\n'
                         '    return 0;\n'
                         '}',
               'desc': 'The break statement immediately terminates the loop it is placed inside, '
                       "regardless of whether the loop's normal continuation condition would "
                       'otherwise still be true. The instant a break statement runs, the program '
                       'abandons every remaining iteration of that loop and jumps straight to the '
                       "first line of code that appears after the loop's closing brace. This is "
                       'especially useful for search operations, where there is no need to keep '
                       'scanning through the rest of a collection once the desired item has '
                       'already been located.'},
 'C++ continue': {'type': 'C++ loops',
                  'syntax': '#include <iostream>\n'
                            'using namespace std;\n'
                            '\n'
                            'int main() {\n'
                            '    string orderStatuses[5] = {"Pending", "Cancelled", "Shipped", '
                            '"Cancelled", "Delivered"};\n'
                            '\n'
                            '    for (int i = 0; i < 5; i++) {\n'
                            '        if (orderStatuses[i] == "Cancelled") {\n'
                            '            cout << "Skipping cancelled order at index " << i << '
                            'endl;\n'
                            '            continue; // Skips the rest of THIS iteration only, then '
                            'moves straight to the next one\n'
                            '        }\n'
                            '        cout << "Processing order: " << orderStatuses[i] << endl;\n'
                            '    }\n'
                            '    return 0;\n'
                            '}',
                  'desc': 'The continue statement skips only the remaining code within the current '
                          "pass of a loop, then immediately jumps back to re-check the loop's "
                          'condition and begin the next iteration, without exiting the loop '
                          'altogether. This differs from break, which stops the loop entirely. '
                          'Continue is useful when certain items in a sequence need to be excluded '
                          'from processing without interrupting the overall repetition, allowing '
                          'the loop to move on cleanly to the next item as if that particular '
                          'iteration had never happened.'},
 'C++ arrays': {'type': 'C++ arrays & structures',
                'syntax': '#include <iostream>\n'
                          'using namespace std;\n'
                          '\n'
                          'int main() {\n'
                          '    int weeklyTemps[5] = {68, 71, 65, 70, 73}; // Fixed-size array of 5 '
                          'integers\n'
                          '\n'
                          '    weeklyTemps[0] = 66;               // Overwrites the first element '
                          'directly by index (index starts at 0)\n'
                          '    int arraySize = sizeof(weeklyTemps) / sizeof(weeklyTemps[0]); // '
                          'Common trick to count elements\n'
                          '\n'
                          '    for (int i = 0; i < arraySize; i++) {\n'
                          '        cout << "Day " << i << ": " << weeklyTemps[i] << " degrees" << '
                          'endl;\n'
                          '    }\n'
                          '    return 0;\n'
                          '}',
                'desc': 'An array is a fixed-size, contiguous block of memory that stores multiple '
                        'values of the same data type under a single variable name. Elements are '
                        'accessed using square-bracket index notation, and indexing always starts '
                        'at 0, meaning the first element is at index 0 and the last is at index '
                        '(size - 1). Once declared, a standard C++ array cannot grow or shrink; '
                        'its size is locked in at creation. Because arrays do not track their own '
                        'length, developers commonly calculate the number of elements using the '
                        "expression 'sizeof(array) / sizeof(array[0])', which divides the total "
                        'memory footprint of the array by the memory footprint of a single '
                        'element.'},
 'C++ multidimensional arrays': {'type': 'C++ arrays & structures',
                                 'syntax': '#include <iostream>\n'
                                           'using namespace std;\n'
                                           '\n'
                                           'int main() {\n'
                                           '    char gameBoard[3][3] = {\n'
                                           "        {'X', 'O', 'X'},\n"
                                           "        {'O', 'X', 'O'},\n"
                                           "        {'X', ' ', 'O'}\n"
                                           '    };\n'
                                           '\n'
                                           "    gameBoard[2][1] = 'X'; // Overwrites row 2, column "
                                           '1 directly\n'
                                           '\n'
                                           '    for (int row = 0; row < 3; row++) {\n'
                                           '        for (int col = 0; col < 3; col++) {\n'
                                           '            cout << gameBoard[row][col] << " ";\n'
                                           '        }\n'
                                           '        cout << endl;\n'
                                           '    }\n'
                                           '    return 0;\n'
                                           '}',
                                 'desc': 'A multidimensional array is essentially an array of '
                                         'arrays, most commonly used as a two-dimensional grid '
                                         'with rows and columns, though C++ supports higher '
                                         'dimensions as well. Each element is accessed using two '
                                         '(or more) index values in sequence, such as '
                                         'gameBoard[row][column], where the first index selects '
                                         'the row and the second selects the position within that '
                                         'row. Multidimensional arrays are especially useful for '
                                         'representing tabular data, grids, matrices, or game '
                                         'boards where information naturally has both a horizontal '
                                         'and vertical position.'},
 'C++ structures': {'type': 'C++ arrays & structures',
                    'syntax': '#include <iostream>\n'
                              '#include <string>\n'
                              'using namespace std;\n'
                              '\n'
                              'struct Employee {\n'
                              '    string fullName;\n'
                              '    int employeeId;\n'
                              '    double annualSalary;\n'
                              '};\n'
                              '\n'
                              'int main() {\n'
                              '    Employee newHire;              // Create a struct instance\n'
                              '    newHire.fullName = "Maria Chen";\n'
                              '    newHire.employeeId = 4471;\n'
                              '    newHire.annualSalary = 68000.0;\n'
                              '\n'
                              '    cout << newHire.fullName << " (ID: " << newHire.employeeId << '
                              '") earns $" << newHire.annualSalary << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': "A structure, declared with the 'struct' keyword, is a way to group "
                            'several related variables of potentially different data types '
                            'together under one custom type name. Once a struct is defined, you '
                            'can create as many instances (objects) of it as needed, and access or '
                            "modify each instance's individual fields using the dot ('.') "
                            'operator. Structures are conceptually similar to classes in C++, with '
                            'the key default difference being that struct members are public by '
                            'default, whereas class members are private by default; structs are '
                            'often used for simple data containers that bundle related values '
                            'without needing complex behavior.'},
 'C++ enums': {'type': 'C++ arrays & structures',
               'syntax': '#include <iostream>\n'
                         'using namespace std;\n'
                         '\n'
                         'enum TrafficLightState { RED, YELLOW, GREEN }; // Defines a fixed set of '
                         'named constants (RED=0, YELLOW=1, GREEN=2)\n'
                         '\n'
                         'int main() {\n'
                         '    TrafficLightState currentSignal = RED;\n'
                         '\n'
                         '    if (currentSignal == RED) {\n'
                         '        cout << "Signal: Stop! Vehicles must halt." << endl;\n'
                         '    }\n'
                         '    return 0;\n'
                         '}',
               'desc': 'An enum (short for enumeration) defines a custom type made up of a fixed, '
                       'named set of integer constants, which makes code far more readable than '
                       'using plain numbers to represent a limited set of states. By default, the '
                       'first name in the list is assigned the value 0, and each subsequent name '
                       'automatically increments by 1, though these underlying values can be '
                       'overridden manually. Enums are particularly useful for representing a '
                       'variable that should only ever hold one of a small, known set of options, '
                       'such as days of the week, directions, or status codes, because they '
                       'prevent invalid or unintended values from being assigned.'},
 'C++ references': {'type': 'C++ references & pointers',
                    'syntax': '#include <iostream>\n'
                              'using namespace std;\n'
                              '\n'
                              'int main() {\n'
                              '    double accountBalance = 500.0;\n'
                              "    double &balanceAlias = accountBalance; // '&' here creates a "
                              'reference: another name for the SAME variable\n'
                              '\n'
                              '    balanceAlias += 150.0; // Modifying the alias modifies the '
                              'original variable too, since they share memory\n'
                              '\n'
                              '    cout << "Original balance: " << accountBalance << " | Via '
                              'alias: " << balanceAlias << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': 'A reference is an alias, meaning another name, for an '
                            'already-existing variable. It is created by placing an ampersand (&) '
                            'after the data type in the declaration, and once bound to a variable, '
                            'a reference cannot be changed to refer to a different variable later. '
                            'Because a reference and its original variable point to the exact same '
                            'location in memory, changing the value through one automatically '
                            'changes the value seen through the other. References are commonly '
                            'used as function parameters to allow a function to modify the '
                            "caller's original variable directly, or to avoid the performance cost "
                            'of copying large objects.'},
 'C++ pointers': {'type': 'C++ references & pointers',
                  'syntax': '#include <iostream>\n'
                            'using namespace std;\n'
                            '\n'
                            'int main() {\n'
                            '    int sensorReading = 72;\n'
                            "    int *sensorPointer = &sensorReading; // '&' gets the memory "
                            "address; '*' declares a pointer variable\n"
                            '\n'
                            '    cout << "Value: " << sensorReading << " | Address: " << '
                            'sensorPointer << endl;\n'
                            '\n'
                            '    *sensorPointer = 85; // Dereferencing (*) accesses/modifies the '
                            'value AT that memory address\n'
                            '    cout << "Updated value via pointer: " << sensorReading << endl;\n'
                            '    return 0;\n'
                            '}',
                  'desc': 'A pointer is a variable that stores the memory address of another '
                          "variable rather than storing a value directly. The '&' operator "
                          'retrieves the memory address of a variable, while declaring a variable '
                          "with an asterisk (*), like 'int *ptr', creates a pointer capable of "
                          'holding such an address. Using the asterisk again on a pointer '
                          'variable, called dereferencing, accesses or modifies the actual value '
                          'stored at that address rather than the address itself. Pointers give '
                          'C++ programmers powerful, low-level control over memory, enabling '
                          'dynamic memory allocation, efficient handling of large data structures, '
                          'and direct manipulation of hardware-level data, though they require '
                          'careful handling to avoid bugs like dereferencing invalid addresses.'},
 'C++ functions': {'type': 'C++ functions',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             '// Function declaration: takes a double parameter, returns a double\n'
                             'double calculateShippingCost(double packageWeightKg) {\n'
                             '    double baseRate = 5.0;\n'
                             '    double perKgRate = 2.5;\n'
                             "    return baseRate + (packageWeightKg * perKgRate); // 'return' "
                             'sends the result back to the caller\n'
                             '}\n'
                             '\n'
                             'int main() {\n'
                             '    double cost = calculateShippingCost(3.2); // Function call, '
                             'passing an argument\n'
                             '    cout << "Shipping cost: $" << cost << endl;\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'A function is a named, reusable block of code designed to perform a '
                           'specific task, which helps organize a program and avoid repeating the '
                           'same logic in multiple places. A function definition specifies a '
                           "return type (the kind of value it sends back, or 'void' if it returns "
                           'nothing), a name, and a parameter list in parentheses describing what '
                           "inputs it expects. Inside the function body, the 'return' keyword "
                           'sends a value back to wherever the function was called from and '
                           "immediately ends the function's execution. Once defined, a function "
                           'can be called (invoked) any number of times throughout the program '
                           'simply by writing its name followed by parentheses containing the '
                           'required arguments.'},
 'C++ function overloading': {'type': 'C++ functions',
                              'syntax': '#include <iostream>\n'
                                        'using namespace std;\n'
                                        '\n'
                                        '// Same function name, different parameter lists -> the '
                                        'compiler picks the right one based on arguments\n'
                                        'double calculateArea(double side) {           // Square: '
                                        'one parameter\n'
                                        '    return side * side;\n'
                                        '}\n'
                                        'double calculateArea(double length, double width) { // '
                                        'Rectangle: two parameters\n'
                                        '    return length * width;\n'
                                        '}\n'
                                        '\n'
                                        'int main() {\n'
                                        '    cout << "Square area: " << calculateArea(4.0) << '
                                        'endl;\n'
                                        '    cout << "Rectangle area: " << calculateArea(4.0, 6.0) '
                                        '<< endl;\n'
                                        '    return 0;\n'
                                        '}',
                              'desc': 'Function overloading allows multiple functions to share the '
                                      'exact same name as long as their parameter lists differ, '
                                      'either in the number of parameters, the data types of the '
                                      'parameters, or both. When an overloaded function is called, '
                                      'the compiler examines the arguments provided at the call '
                                      'site and automatically selects the matching version to '
                                      'execute, a process resolved entirely at compile time. This '
                                      'technique lets programmers use one intuitive, consistent '
                                      'name for a family of closely related operations, rather '
                                      'than inventing separate names like calculateSquareArea and '
                                      'calculateRectangleArea for what is conceptually the same '
                                      'underlying task.'},
 'C++ recursion': {'type': 'C++ functions',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             'int calculateFactorial(int n) {\n'
                             '    if (n <= 1) {          // Base case: stops the recursion from '
                             'continuing forever\n'
                             '        return 1;\n'
                             '    }\n'
                             '    return n * calculateFactorial(n - 1); // Recursive case: '
                             'function calls itself with a smaller input\n'
                             '}\n'
                             '\n'
                             'int main() {\n'
                             '    cout << "5! = " << calculateFactorial(5) << endl; // Prints 120\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Recursion is a technique where a function calls itself in order to '
                           'solve a problem by breaking it down into smaller, self-similar '
                           'sub-problems. Every properly designed recursive function needs two '
                           'essential parts: a base case, which is a condition that stops the '
                           'recursive calls once the simplest version of the problem has been '
                           'reached, and a recursive case, where the function calls itself again '
                           'with input that moves progressively closer to that base case. Without '
                           'a correctly defined base case, a recursive function will call itself '
                           "indefinitely until it exhausts the program's call stack, causing a "
                           'crash known as a stack overflow.'},
 'C++ classes and objects': {'type': 'C++ classes',
                             'syntax': '// --- CLASS DEFINITION (Blueprint) ---\n'
                                       '#include <iostream>\n'
                                       '#include <string>\n'
                                       'using namespace std;\n'
                                       '\n'
                                       'class Car {\n'
                                       '  public:\n'
                                       '    string brand;\n'
                                       '    void honk() {\n'
                                       '        cout << brand << " says: Beep beep!" << endl;\n'
                                       '    }\n'
                                       '};\n'
                                       '\n'
                                       '// --- HOW TO CALL/USE IT (Object Instantiation) ---\n'
                                       'int main() {\n'
                                       '    Car myCar;              // Create an object (instance) '
                                       'of the Car class\n'
                                       '    myCar.brand = "Toyota"; // Access a public field\n'
                                       '    myCar.honk();           // Call a public method\n'
                                       '    return 0;\n'
                                       '}',
                             'desc': 'A class is a user-defined blueprint that bundles together '
                                     'data (called attributes or member variables) and behaviors '
                                     '(called member functions or methods) into a single reusable '
                                     'structure. Once a class is defined, you create actual usable '
                                     'instances of it, called objects, using the class name as if '
                                     'it were a data type. Each object created from the same class '
                                     "has its own independent copy of the class's member "
                                     'variables, but shares the same defined methods. The dot '
                                     "('.') operator is used to access an object's public members, "
                                     "such as setting a field's value or calling one of its "
                                     'functions.'},
 'C++ class methods': {'type': 'C++ classes',
                       'syntax': '#include <iostream>\n'
                                 'using namespace std;\n'
                                 '\n'
                                 'class Router {\n'
                                 '  public:\n'
                                 '    string ipAddress;\n'
                                 '\n'
                                 '    void connect() { // Regular member method, operates on THIS '
                                 "specific object's data\n"
                                 '        cout << "Connecting router at " << ipAddress << endl;\n'
                                 '    }\n'
                                 '    static void printManufacturer() { // Static method: belongs '
                                 'to the class itself, not any one object\n'
                                 '        cout << "Manufactured by NetCore Systems." << endl;\n'
                                 '    }\n'
                                 '};\n'
                                 '\n'
                                 'int main() {\n'
                                 '    Router homeRouter;\n'
                                 '    homeRouter.ipAddress = "192.168.1.1";\n'
                                 '    homeRouter.connect();\n'
                                 '    Router::printManufacturer(); // Called using the class name '
                                 "and '::', no object required\n"
                                 '    return 0;\n'
                                 '}',
                       'desc': 'Class methods define what actions or behaviors the objects of a '
                               'class can perform, and are written the same way as regular '
                               'functions but declared inside the class body. Regular (instance) '
                               'methods operate on the specific data belonging to the individual '
                               'object that calls them, accessed through the dot operator. Static '
                               "methods, marked with the 'static' keyword, belong to the class as "
                               'a whole rather than to any single object; they are called using '
                               "the class name followed by the scope resolution operator '::', and "
                               'they cannot access non-static member variables since they are not '
                               'tied to a particular instance.'},
 'C++ constructors': {'type': 'C++ classes',
                      'syntax': '#include <iostream>\n'
                                '#include <string>\n'
                                'using namespace std;\n'
                                '\n'
                                'class BankAccount {\n'
                                '  public:\n'
                                '    string ownerName;\n'
                                '    double balance;\n'
                                '\n'
                                '    BankAccount(string name, double startingBalance) { // '
                                'Constructor: same name as the class, no return type\n'
                                '        ownerName = name;\n'
                                '        balance = startingBalance;\n'
                                '        cout << "Account created for " << ownerName << endl;\n'
                                '    }\n'
                                '};\n'
                                '\n'
                                'int main() {\n'
                                '    BankAccount account("Sam Lee", 250.0); // Constructor runs '
                                'automatically here\n'
                                '    cout << account.ownerName << " has $" << account.balance << '
                                'endl;\n'
                                '    return 0;\n'
                                '}',
                      'desc': 'A constructor is a special member function that shares the exact '
                              'same name as its class and has no return type, not even void. It '
                              'runs automatically the very instant a new object of that class is '
                              'created, making it the ideal place to initialize member variables '
                              'with starting values or run any setup logic the object requires '
                              'before it is used. Constructors can accept parameters just like '
                              'regular functions, allowing each object to be initialized with '
                              'different starting data at the moment of its creation, as shown by '
                              'passing a name and starting balance directly into the BankAccount '
                              'constructor above.'},
 'C++ access specifiers': {'type': 'C++ classes',
                           'syntax': '#include <iostream>\n'
                                     'using namespace std;\n'
                                     '\n'
                                     'class SecurityPanel {\n'
                                     '  private:\n'
                                     '    string masterCode = "7729"; // Only accessible from '
                                     'inside this class\n'
                                     '\n'
                                     '  public:\n'
                                     '    string displayStatus = "Armed"; // Accessible from '
                                     'anywhere the object is visible\n'
                                     '\n'
                                     '    void verifyCode(string inputCode) {\n'
                                     '        if (inputCode == masterCode) { // Private members '
                                     "ARE accessible from within the class's own methods\n"
                                     '            cout << "Access granted." << endl;\n'
                                     '        } else {\n'
                                     '            cout << "Access denied." << endl;\n'
                                     '        }\n'
                                     '    }\n'
                                     '};\n'
                                     '\n'
                                     'int main() {\n'
                                     '    SecurityPanel panel;\n'
                                     '    cout << panel.displayStatus << endl; // OK: public '
                                     'member\n'
                                     '    panel.verifyCode("7729");             // OK: calling a '
                                     'public method\n'
                                     '    // cout << panel.masterCode;          // ERROR: '
                                     'masterCode is private, inaccessible from outside\n'
                                     '    return 0;\n'
                                     '}',
                           'desc': 'Access specifiers control which parts of a program are allowed '
                                   "to see or interact with a class's members. 'public' members "
                                   'are accessible from anywhere the object itself is visible, '
                                   "including outside code. 'private' members, which is also the "
                                   'default visibility for anything in a class if no specifier is '
                                   "written, can only be accessed from within the class's own "
                                   'member functions, hiding them completely from outside code. A '
                                   "third specifier, 'protected', behaves like private but "
                                   'additionally allows access from any classes that inherit from '
                                   'it. Using access specifiers deliberately is central to '
                                   'designing safe, well-encapsulated classes.'},
 'C++ encapsulation': {'type': 'C++ classes',
                       'syntax': '#include <iostream>\n'
                                 'using namespace std;\n'
                                 '\n'
                                 'class Wallet {\n'
                                 '  private:\n'
                                 '    double cashBalance = 100.0; // Hidden from direct outside '
                                 'access\n'
                                 '\n'
                                 '  public:\n'
                                 '    double getBalance() {       // Getter: safe, read-only '
                                 'access to the private field\n'
                                 '        return cashBalance;\n'
                                 '    }\n'
                                 '    void deposit(double amount) { // Setter with built-in '
                                 'validation logic\n'
                                 '        if (amount > 0) {\n'
                                 '            cashBalance += amount;\n'
                                 '        } else {\n'
                                 '            cout << "Invalid deposit amount rejected." << endl;\n'
                                 '        }\n'
                                 '    }\n'
                                 '};\n'
                                 '\n'
                                 'int main() {\n'
                                 '    Wallet myWallet;\n'
                                 '    myWallet.deposit(50.0);\n'
                                 '    cout << "Balance: " << myWallet.getBalance() << endl;\n'
                                 '    return 0;\n'
                                 '}',
                       'desc': "Encapsulation is the object-oriented practice of hiding a class's "
                               'internal data from direct outside access, typically by marking '
                               'member variables as private, and instead exposing controlled '
                               'public methods, commonly called getters and setters, to read or '
                               'modify that data. This approach allows the class to enforce '
                               'validation rules or business logic every time its data changes, '
                               'preventing external code from putting an object into an invalid or '
                               'inconsistent state. Encapsulation also means the internal '
                               'implementation of a class can be changed later without breaking '
                               'any outside code that relies on it, as long as the public '
                               'interface stays the same.'},
 'C++ inheritance': {'type': 'C++ classes',
                     'syntax': '#include <iostream>\n'
                               'using namespace std;\n'
                               '\n'
                               'class Vehicle { // Base (parent) class\n'
                               '  public:\n'
                               '    int wheelCount = 4;\n'
                               '    void startEngine() {\n'
                               '        cout << "Engine starting..." << endl;\n'
                               '    }\n'
                               '};\n'
                               '\n'
                               'class SportsCar : public Vehicle { // Derived (child) class, '
                               "inherits from Vehicle using ':'\n"
                               '  public:\n'
                               '    int topSpeedMph = 180;\n'
                               '    void engageTurbo() {\n'
                               '        startEngine();  // Inherited method, available '
                               'automatically\n'
                               '        cout << "Turbo engaged! Top speed: " << topSpeedMph << " '
                               'mph" << endl;\n'
                               '    }\n'
                               '};\n'
                               '\n'
                               'int main() {\n'
                               '    SportsCar ferrari;\n'
                               '    ferrari.engageTurbo();\n'
                               '    return 0;\n'
                               '}',
                     'desc': 'Inheritance allows a new class, called the derived (or child) class, '
                             'to automatically acquire the member variables and methods of an '
                             'existing class, called the base (or parent) class, by writing a '
                             'colon followed by an access specifier and the parent class name '
                             'after the child class name. This mechanism prevents code duplication '
                             'by letting shared, general-purpose behavior live in one base class '
                             'while each derived class adds only the specialized features unique '
                             'to it. The derived class can use inherited members directly as '
                             'though it had defined them itself, and can also add entirely new '
                             'members or override inherited ones to customize behavior.'},
 'C++ polymorphism': {'type': 'C++ classes',
                      'syntax': '#include <iostream>\n'
                                'using namespace std;\n'
                                '\n'
                                'class AudioTrack {\n'
                                '  public:\n'
                                "    virtual void play() { // 'virtual' allows this method to be "
                                'overridden by derived classes\n'
                                '        cout << "Playing generic audio stream..." << endl;\n'
                                '    }\n'
                                '};\n'
                                '\n'
                                'class Mp3Track : public AudioTrack {\n'
                                '  public:\n'
                                "    void play() override { // 'override' explicitly marks this as "
                                "replacing the parent's version\n"
                                '        cout << "Playing compressed MP3 track with equalizer '
                                'settings." << endl;\n'
                                '    }\n'
                                '};\n'
                                '\n'
                                'int main() {\n'
                                '    AudioTrack *nowPlaying = new Mp3Track(); // A base class '
                                'pointer referring to a derived class object\n'
                                "    nowPlaying->play(); // Runs Mp3Track's version, not "
                                "AudioTrack's, thanks to polymorphism\n"
                                '    delete nowPlaying;\n'
                                '    return 0;\n'
                                '}',
                      'desc': "Polymorphism, meaning 'many forms', is the ability for a base class "
                              "pointer or reference to invoke a derived class's own version of a "
                              "method rather than the base class's version, chosen dynamically at "
                              'runtime based on the actual object type being pointed to. To enable '
                              'this behavior, the base class method must be declared with the '
                              "'virtual' keyword, signaling to the compiler that derived classes "
                              'are allowed to override it, and the derived class typically marks '
                              "its version with the 'override' keyword for clarity and safety. "
                              'Polymorphism allows code to work generically with a whole family of '
                              'related objects through a common base type, while each specific '
                              'object still behaves according to its own specialized '
                              'implementation.'},
 'C++ exceptions': {'type': 'C++ errors',
                    'syntax': '#include <iostream>\n'
                              'using namespace std;\n'
                              '\n'
                              'double safeDivide(double numerator, double denominator) {\n'
                              '    if (denominator == 0) {\n'
                              '        throw runtime_error("Division by zero is not allowed."); // '
                              "'throw' raises an exception\n"
                              '    }\n'
                              '    return numerator / denominator;\n'
                              '}\n'
                              '\n'
                              'int main() {\n'
                              '    try {\n'
                              '        double result = safeDivide(10.0, 0.0); // Risky code goes '
                              "inside 'try'\n"
                              '        cout << "Result: " << result << endl;\n'
                              '    } catch (const runtime_error &e) { // Catches the specific '
                              'exception type thrown\n'
                              '        cout << "Error caught: " << e.what() << endl; // e.what() '
                              'returns the error message\n'
                              '    }\n'
                              '    cout << "Program continues running safely after the error." << '
                              'endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': "Exceptions are C++'s mechanism for handling runtime errors gracefully "
                            'rather than allowing them to crash the entire program. Risky code, '
                            'meaning code that might fail under certain conditions, is placed '
                            "inside a 'try' block. If a problem occurs, the 'throw' keyword raises "
                            '(throws) an exception object describing the error, which immediately '
                            'halts normal execution of the try block and hands control over to a '
                            "matching 'catch' block designed to handle that particular exception "
                            'type. The standard <stdexcept> header provides common built-in '
                            "exception types, and their '.what()' method returns a human-readable "
                            'description of what went wrong, allowing the program to log the issue '
                            'and continue running instead of terminating abruptly.'},
 'C++ file handling': {'type': 'C++ file handling',
                       'syntax': '#include <iostream>\n'
                                 '#include <fstream>  // Required for file stream operations\n'
                                 '#include <string>\n'
                                 'using namespace std;\n'
                                 '\n'
                                 'int main() {\n'
                                 '    // Writing to a file\n'
                                 '    ofstream logWriter("system_log.txt"); // Opens (or creates) '
                                 'a file for writing\n'
                                 '    if (logWriter.is_open()) {\n'
                                 '        logWriter << "System check: OK" << endl;\n'
                                 "        logWriter.close(); // Always close a file once you're "
                                 'done with it\n'
                                 '    }\n'
                                 '\n'
                                 '    // Reading from a file\n'
                                 '    ifstream logReader("system_log.txt");\n'
                                 '    string lineContent;\n'
                                 '    if (logReader.is_open()) {\n'
                                 '        while (getline(logReader, lineContent)) { // Reads the '
                                 'file line by line until it runs out\n'
                                 '            cout << "Read from file: " << lineContent << endl;\n'
                                 '        }\n'
                                 '        logReader.close();\n'
                                 '    }\n'
                                 '    return 0;\n'
                                 '}',
                       'desc': 'File handling in C++ is performed through the <fstream> header, '
                               'which provides three main stream classes: ofstream for writing '
                               'output to files, ifstream for reading input from files, and '
                               'fstream for both reading and writing through a single stream. '
                               'Opening a file is typically done by constructing one of these '
                               "stream objects with the target filename; the '.is_open()' method "
                               'checks whether that operation succeeded before attempting to use '
                               "the file. Data is written to a file using the same '<<' insertion "
                               'operator used for console output, and commonly read back line by '
                               "line using the 'getline()' function inside a loop. It is important "
                               "practice to call '.close()' on a file stream once finished, which "
                               'releases the file and ensures any buffered data is properly saved '
                               'to disk.'},
 'C++ vectors': {'type': 'C++ data structures & STL',
                 'syntax': '#include <iostream>\n'
                           '#include <vector>\n'
                           '#include <string>\n'
                           'using namespace std;\n'
                           '\n'
                           'int main() {\n'
                           '    vector<string> playlist = {"Song A", "Song B"};\n'
                           '\n'
                           '    playlist.push_back("Song C");   // Adds an item to the end, '
                           'growing the vector automatically\n'
                           '    playlist[0] = "Song A (Remix)"; // Direct index '
                           'access/modification, just like an array\n'
                           '    playlist.pop_back();            // Removes the last item\n'
                           '\n'
                           '    for (const string &track : playlist) {\n'
                           '        cout << "Now queued: " << track << endl;\n'
                           '    }\n'
                           '    cout << "Total tracks: " << playlist.size() << endl; // size() '
                           'returns the current element count\n'
                           '    return 0;\n'
                           '}',
                 'desc': 'A vector, provided by the <vector> header, is a dynamic array from the '
                         'C++ Standard Template Library (STL) that can automatically grow or '
                         'shrink in size as elements are added or removed, unlike a fixed-size '
                         'plain array. Elements can be accessed and modified using the same '
                         'square-bracket index notation as arrays, added to the end efficiently '
                         'with push_back(), removed from the end with pop_back(), and the current '
                         'number of stored elements can be checked at any time with the size() '
                         'method. Because of this flexibility, vectors are one of the most '
                         'commonly used containers in modern C++ whenever the number of elements '
                         'needed is not known in advance.'},
 'C++ list': {'type': 'C++ data structures & STL',
              'syntax': '#include <iostream>\n'
                        '#include <list>\n'
                        '#include <string>\n'
                        'using namespace std;\n'
                        '\n'
                        'int main() {\n'
                        '    list<string> taskQueue = {"Design", "Test", "Deploy"};\n'
                        '\n'
                        '    taskQueue.push_front("Plan");  // Efficiently adds an item to the '
                        'very front\n'
                        '    taskQueue.push_back("Review");  // Adds an item to the end\n'
                        '\n'
                        '    for (const string &task : taskQueue) {\n'
                        '        cout << "Task: " << task << endl;\n'
                        '    }\n'
                        '    return 0;\n'
                        '}',
              'desc': 'A list, from the <list> header, is a doubly-linked list container from the '
                      'STL, meaning each element keeps track of both the element before it and the '
                      'element after it. Unlike a vector, a list does not support fast direct '
                      'index access, but it excels at inserting or removing elements from any '
                      'position, especially the front or middle, in constant time without shifting '
                      'the rest of the elements around in memory. This makes list a strong choice '
                      'for scenarios involving frequent insertions and deletions throughout a '
                      'sequence, such as task queues or playlists that are reordered often, rather '
                      'than scenarios needing fast random access by index.'},
 'C++ stacks': {'type': 'C++ data structures & STL',
                'syntax': '#include <iostream>\n'
                          '#include <stack>\n'
                          '#include <string>\n'
                          'using namespace std;\n'
                          '\n'
                          'int main() {\n'
                          '    stack<string> browserHistory;\n'
                          '\n'
                          '    browserHistory.push("home.com");    // Adds an item to the TOP of '
                          'the stack\n'
                          '    browserHistory.push("products.com");\n'
                          '    browserHistory.push("checkout.com");\n'
                          '\n'
                          '    cout << "Current page: " << browserHistory.top() << endl; // top() '
                          'peeks at the most recently added item\n'
                          "    browserHistory.pop(); // Removes the top item (going 'back' one "
                          'page)\n'
                          '    cout << "After going back: " << browserHistory.top() << endl;\n'
                          '    return 0;\n'
                          '}',
                'desc': 'A stack, from the <stack> header, is a container that follows '
                        'Last-In-First-Out (LIFO) ordering, meaning the most recently added '
                        'element is always the first one to be removed, much like a physical stack '
                        'of plates. The push() method adds a new element to the top of the stack, '
                        'pop() removes the top element without returning it, and top() lets you '
                        'look at (peek) the current top element without removing it. Stacks are a '
                        "natural fit for problems that mirror this 'undo the most recent action "
                        "first' pattern, such as browser back-button history, undo functionality "
                        'in editors, or tracking function calls.'},
 'C++ queues': {'type': 'C++ data structures & STL',
                'syntax': '#include <iostream>\n'
                          '#include <queue>\n'
                          '#include <string>\n'
                          'using namespace std;\n'
                          '\n'
                          'int main() {\n'
                          '    queue<string> supportTickets;\n'
                          '\n'
                          '    supportTickets.push("Ticket #101"); // Adds to the BACK of the '
                          'queue\n'
                          '    supportTickets.push("Ticket #102");\n'
                          '    supportTickets.push("Ticket #103");\n'
                          '\n'
                          '    cout << "Now serving: " << supportTickets.front() << endl; // '
                          'front() peeks at the next item to be served\n'
                          '    supportTickets.pop(); // Removes the item at the front\n'
                          '    cout << "Next up: " << supportTickets.front() << endl;\n'
                          '    return 0;\n'
                          '}',
                'desc': 'A queue, from the <queue> header, is a container that follows '
                        'First-In-First-Out (FIFO) ordering, meaning the first element added is '
                        'also the first one to be removed, much like people waiting in a checkout '
                        'line. The push() method adds a new element to the back of the queue, '
                        'pop() removes the element currently at the front, and front() lets you '
                        'view that next element without removing it. Queues are the natural choice '
                        'whenever items need to be processed strictly in the order they arrived, '
                        'such as print job scheduling, customer support ticket handling, or task '
                        'processing pipelines.'},
 'C++ maps': {'type': 'C++ data structures & STL',
              'syntax': '#include <iostream>\n'
                        '#include <map>\n'
                        '#include <string>\n'
                        'using namespace std;\n'
                        '\n'
                        'int main() {\n'
                        '    map<string, double> productPrices;\n'
                        '\n'
                        '    productPrices["SKU-001"] = 19.99; // Adds or updates a key-value pair '
                        'directly\n'
                        '    productPrices["SKU-002"] = 45.50;\n'
                        '    productPrices.insert({"SKU-003", 8.75}); // Alternative way to insert '
                        'a new pair\n'
                        '\n'
                        "    for (const auto &entry : productPrices) { // 'auto' lets the compiler "
                        'infer the pair type\n'
                        '        cout << entry.first << " costs $" << entry.second << endl;\n'
                        '    }\n'
                        '    productPrices.erase("SKU-001"); // Removes a key-value pair by its '
                        'key\n'
                        '    return 0;\n'
                        '}',
              'desc': 'A map, from the <map> header, is an associative container from the STL that '
                      'stores data as key-value pairs, where each unique key maps to exactly one '
                      'value, similar to a real-world dictionary. Internally, a map automatically '
                      'keeps its keys sorted. New entries can be added or existing ones updated '
                      'using square-bracket key access, similar to arrays but with a key instead '
                      'of a numeric index, or through the insert() method for adding new pairs. '
                      'The erase() method removes an entry by its key, and iterating over a map '
                      "yields pairs where '.first' refers to the key and '.second' refers to the "
                      'associated value. Maps are ideal whenever data needs to be looked up '
                      'quickly by a meaningful identifier rather than a sequential position.'},
 'C++ iterators': {'type': 'C++ data structures & STL',
                   'syntax': '#include <iostream>\n'
                             '#include <vector>\n'
                             'using namespace std;\n'
                             '\n'
                             'int main() {\n'
                             '    vector<int> examScores = {88, 72, 95, 60};\n'
                             '\n'
                             '    // begin() points to the first element, end() points PAST the '
                             'last element\n'
                             '    for (vector<int>::iterator it = examScores.begin(); it != '
                             'examScores.end(); ++it) {\n'
                             '        if (*it < 70) {   // Dereferencing (*it) accesses the value '
                             'the iterator currently points to\n'
                             '            *it += 5;      // Curve up any failing score directly '
                             'through the iterator\n'
                             '        }\n'
                             '        cout << "Score: " << *it << endl;\n'
                             '    }\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'An iterator is an object that behaves like a generalized pointer, '
                           'designed to move through the elements of an STL container such as a '
                           'vector, list, or map in a consistent, uniform way regardless of how '
                           'that container stores its data internally. The begin() method returns '
                           'an iterator pointing to the first element, while end() returns an '
                           'iterator pointing to the position just past the last element, marking '
                           'the boundary used to know when to stop. Iterators are advanced using '
                           'the increment operator (++it) and are dereferenced with the asterisk '
                           '(*it) to access or modify the value at their current position, making '
                           'them the standard tool STL algorithms use to traverse and process '
                           'containers.'},
 'C++ user input': {'type': 'C++ variables & data types',
                    'syntax': '#include <iostream>\n'
                              '#include <string>\n'
                              'using namespace std;\n'
                              '\n'
                              'int main() {\n'
                              '    string userName;\n'
                              '    int userAge;\n'
                              '\n'
                              '    cout << "Enter your name: ";\n'
                              '    cin >> userName;                 // cin reads a single word of '
                              'input from the keyboard\n'
                              '    cout << "Enter your age: ";\n'
                              '    cin >> userAge;\n'
                              '\n'
                              '    cout << "Hello " << userName << ", age " << userAge << endl;\n'
                              '\n'
                              '    cin.ignore();                    // Clears the leftover newline '
                              'character left in the input buffer\n'
                              '    string fullSentence;\n'
                              '    cout << "Enter a full sentence: ";\n'
                              '    getline(cin, fullSentence);      // getline() reads an entire '
                              'line, including spaces\n'
                              '    cout << "You said: " << fullSentence << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': "User input is captured from the keyboard using 'cin', the standard "
                            'input stream object provided by the <iostream> header, together with '
                            "the extraction operator '>>'. The '>>' operator reads input up until "
                            'it hits whitespace, which means it only captures a single word at a '
                            'time and cannot read a full line containing spaces. To read an entire '
                            'line of text, including spaces, the getline() function is used '
                            'instead, taking cin and the destination string as arguments. Because '
                            "'>>' leaves a trailing newline character behind in the input buffer, "
                            'it is common practice to call cin.ignore() before a getline() call to '
                            'clear that buffer and avoid the getline() immediately capturing an '
                            'empty line.'},
 'C++ namespaces': {'type': 'C++ variables & data types',
                    'syntax': '#include <iostream>\n'
                              "using namespace std;   // Brings every name from the 'std' "
                              "namespace into scope, so 'cout' works without 'std::'\n"
                              '\n'
                              'namespace CompanyTools {\n'
                              '    int calculateBonus(int baseSalary) {\n'
                              '        return baseSalary / 10;\n'
                              '    }\n'
                              '}\n'
                              '\n'
                              'int main() {\n'
                              '    // Calling a function inside a custom namespace using the scope '
                              "resolution operator '::'\n"
                              '    int bonus = CompanyTools::calculateBonus(50000);\n'
                              '    cout << "Bonus: " << bonus << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': 'A namespace is a declarative region that groups related names, such '
                            'as classes, functions, and variables, under one shared label to '
                            'prevent naming collisions between different parts of a large program '
                            'or between different libraries. The C++ Standard Library itself '
                            'places all of its built-in functionality inside a namespace called '
                            "'std', which is why standard tools like cout and cin are technically "
                            "named std::cout and std::cin. Writing 'using namespace std;' at the "
                            'top of a file tells the compiler to assume the std namespace whenever '
                            'a name is not otherwise qualified, saving the need to prefix every '
                            "standard library name with 'std::'. Programmers can also define their "
                            'own custom namespaces to organize their own code and avoid conflicts '
                            'with names used elsewhere.'},
 'C++ templates': {'type': 'C++ functions',
                   'syntax': '#include <iostream>\n'
                             'using namespace std;\n'
                             '\n'
                             '// A template lets ONE function definition work with multiple data '
                             'types\n'
                             'template <typename T>\n'
                             'T findLarger(T firstValue, T secondValue) {\n'
                             '    return (firstValue > secondValue) ? firstValue : secondValue;\n'
                             '}\n'
                             '\n'
                             'int main() {\n'
                             '    cout << "Larger int: " << findLarger(15, 42) << endl;         // '
                             'T becomes int\n'
                             '    cout << "Larger double: " << findLarger(3.7, 2.1) << endl;    // '
                             'T becomes double\n'
                             '    return 0;\n'
                             '}',
                   'desc': 'Templates allow a single function or class definition to work '
                           'generically with any data type, instead of writing separate, nearly '
                           'identical versions of the same logic for int, double, string, and so '
                           "on. A template is declared by writing 'template <typename T>' directly "
                           'above the function, where T acts as a placeholder standing in for '
                           'whatever actual type is used when the template is called. When the '
                           'function is invoked, the compiler automatically determines the correct '
                           'type to substitute for T based on the arguments provided, and '
                           'generates a specific version of that function for that type behind the '
                           'scenes. Templates are a core building block of the C++ Standard '
                           'Library, and are what allow containers like vector and map to store '
                           'any data type.'},
 'C++ deque': {'type': 'C++ data structures & STL',
               'syntax': '#include <iostream>\n'
                         '#include <deque>\n'
                         '#include <string>\n'
                         'using namespace std;\n'
                         '\n'
                         'int main() {\n'
                         '    deque<string> printQueue = {"Report.pdf", "Invoice.pdf"};\n'
                         '\n'
                         '    printQueue.push_back("Memo.pdf");   // Adds an item to the back\n'
                         '    printQueue.push_front("Urgent.pdf"); // Adds an item to the front, '
                         'efficiently\n'
                         '\n'
                         '    for (const string &doc : printQueue) {\n'
                         '        cout << "Queued document: " << doc << endl;\n'
                         '    }\n'
                         '\n'
                         '    printQueue.pop_front(); // Removes the item at the front\n'
                         '    return 0;\n'
                         '}',
               'desc': "A deque (pronounced 'deck', short for double-ended queue), from the "
                       '<deque> header, is a container that allows fast insertion and removal of '
                       'elements at both its front and back, unlike a vector which is only '
                       'efficient for adding or removing elements at the end. It also supports '
                       'direct index access using square brackets, similar to a vector. Because it '
                       'combines efficient two-sided growth with fast random access, a deque is '
                       'useful whenever a program needs to add or remove items from both ends of a '
                       'sequence frequently, such as a sliding window buffer or a print job queue '
                       'where urgent items can be pushed to the front.'},
 'C++ sets': {'type': 'C++ data structures & STL',
              'syntax': '#include <iostream>\n'
                        '#include <set>\n'
                        '#include <string>\n'
                        'using namespace std;\n'
                        '\n'
                        'int main() {\n'
                        '    set<string> registeredEmails;\n'
                        '\n'
                        '    registeredEmails.insert("a@mail.com");\n'
                        '    registeredEmails.insert("b@mail.com");\n'
                        '    registeredEmails.insert("a@mail.com"); // Duplicate: ignored '
                        'automatically, sets only allow unique values\n'
                        '\n'
                        '    if (registeredEmails.count("a@mail.com") > 0) { // count() returns 1 '
                        'if found, 0 if not\n'
                        '        cout << "Email already registered." << endl;\n'
                        '    }\n'
                        '\n'
                        '    for (const string &email : registeredEmails) { // Elements are '
                        'automatically kept in sorted order\n'
                        '        cout << "Registered: " << email << endl;\n'
                        '    }\n'
                        '    return 0;\n'
                        '}',
              'desc': 'A set, from the <set> header, is an associative container that stores a '
                      'collection of unique values, meaning any attempt to insert a duplicate '
                      'value is silently ignored. Internally, a set automatically keeps its '
                      'elements sorted at all times. The insert() method adds a new value, and the '
                      'count() method is the standard way to check whether a particular value '
                      'already exists in the set, since it returns 1 if the value is present and 0 '
                      'if it is not. Sets are ideal whenever a program needs to guarantee that a '
                      'collection contains no repeated items, such as tracking unique visitor IDs '
                      'or filtering out duplicate entries from a list.'},
 'C++ algorithms': {'type': 'C++ data structures & STL',
                    'syntax': '#include <iostream>\n'
                              '#include <vector>\n'
                              '#include <algorithm>   // Required for sort, reverse, find, '
                              'max_element, etc.\n'
                              'using namespace std;\n'
                              '\n'
                              'int main() {\n'
                              '    vector<int> testScores = {88, 45, 97, 62, 73};\n'
                              '\n'
                              '    sort(testScores.begin(), testScores.end());              // '
                              'Sorts the range in ascending order\n'
                              '    reverse(testScores.begin(), testScores.end());            // '
                              'Reverses the current order of the range\n'
                              '\n'
                              '    auto highestScore = max_element(testScores.begin(), '
                              'testScores.end()); // Returns an iterator to the largest element\n'
                              '    auto found = find(testScores.begin(), testScores.end(), '
                              '62);           // Searches for a specific value\n'
                              '\n'
                              '    cout << "Highest score: " << *highestScore << endl;\n'
                              '    cout << "Found 62: " << (found != testScores.end()) << endl;\n'
                              '    return 0;\n'
                              '}',
                    'desc': 'The <algorithm> header provides a large collection of ready-made, '
                            'general-purpose functions from the STL that operate on ranges of '
                            "elements defined by a pair of iterators, typically a container's "
                            'begin() and end(). Common examples include sort(), which arranges a '
                            'range into ascending order; reverse(), which flips the order of a '
                            'range; find(), which searches for the first occurrence of a value and '
                            'returns an iterator to it (or to end() if nothing is found); and '
                            'max_element() / min_element(), which return an iterator pointing to '
                            'the largest or smallest value in the range. Because these algorithms '
                            'work through iterators rather than being tied to one specific '
                            'container type, the exact same sort() or find() call works whether it '
                            'is applied to a vector, a deque, or many other STL containers.'},
 'C++ operator overloading': {'type': 'C++ classes',
                              'syntax': '#include <iostream>\n'
                                        'using namespace std;\n'
                                        '\n'
                                        'class Point2D {\n'
                                        '  public:\n'
                                        '    int x, y;\n'
                                        '    Point2D(int xVal, int yVal) : x(xVal), y(yVal) {}\n'
                                        '\n'
                                        "    // Overloading '+' so two Point2D objects can be "
                                        'added together directly\n'
                                        '    Point2D operator+(const Point2D &other) {\n'
                                        '        return Point2D(x + other.x, y + other.y);\n'
                                        '    }\n'
                                        '};\n'
                                        '\n'
                                        'int main() {\n'
                                        '    Point2D pointA(2, 3);\n'
                                        '    Point2D pointB(4, 1);\n'
                                        '    Point2D result = pointA + pointB; // Calls the '
                                        "overloaded '+' operator automatically\n"
                                        '\n'
                                        '    cout << "Result: (" << result.x << ", " << result.y '
                                        '<< ")" << endl;\n'
                                        '    return 0;\n'
                                        '}',
                              'desc': 'Operator overloading allows a class to redefine what a '
                                      'standard operator, such as +, -, ==, or <<, does when it is '
                                      'used with objects of that class, instead of only working on '
                                      'built-in types like int or double. It is implemented by '
                                      "writing a special member function named 'operator' followed "
                                      "by the symbol being overloaded, such as 'operator+', which "
                                      'specifies exactly what should happen when that operator is '
                                      "applied to the class's objects. This lets custom types "
                                      'behave in an intuitive, natural way that mirrors how '
                                      'built-in types are used, for example allowing two custom '
                                      "Point2D objects to be combined simply by writing 'pointA + "
                                      "pointB' rather than calling a differently-named method."},
 'C++ default parameters': {'type': 'C++ functions',
                            'syntax': '#include <iostream>\n'
                                      'using namespace std;\n'
                                      '\n'
                                      '// discountPercent has a default value, used automatically '
                                      'if the caller omits that argument\n'
                                      'double calculatePrice(double basePrice, double '
                                      'discountPercent = 0.0) {\n'
                                      '    return basePrice - (basePrice * discountPercent / '
                                      '100);\n'
                                      '}\n'
                                      '\n'
                                      'int main() {\n'
                                      '    cout << "No discount: " << calculatePrice(100.0) << '
                                      'endl;         // Uses the default value 0.0\n'
                                      '    cout << "With discount: " << calculatePrice(100.0, '
                                      '20.0) << endl; // Overrides the default with 20.0\n'
                                      '    return 0;\n'
                                      '}',
                            'desc': 'A default parameter is a function parameter that is '
                                    "automatically assigned a fallback value in the function's "
                                    'declaration, which is used whenever the caller does not '
                                    'explicitly supply an argument for that parameter. Default '
                                    'values are specified using an equals sign directly in the '
                                    'parameter list, and any parameters with default values must '
                                    'appear after all parameters that do not have one. This '
                                    'feature lets a single function support both simple calls, '
                                    'where sensible defaults are used, and more detailed calls, '
                                    'where the caller overrides specific values, without needing '
                                    'to write multiple overloaded versions of the same function.'}}
